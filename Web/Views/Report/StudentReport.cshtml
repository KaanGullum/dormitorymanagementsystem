@model StudentReportViewModel
@{
    ViewData["Title"] = "Student Report";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h3 class="fw-bold mb-0">Student Report</h3>
        <p class="text-muted mb-0">Registered student list with filtering and search support</p>
    </div>
    <div class="d-flex gap-2">
        <button type="button" class="btn btn-outline-secondary btn-sm" onclick="window.print()">
            <i class="bi bi-printer me-1"></i>Print
        </button>
        <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
            <i class="bi bi-arrow-left me-1"></i>Reports
        </a>
    </div>
</div>

<div class="card border-0 shadow-sm mb-3">
    <div class="card-body py-2">
        <form asp-action="StudentReport" method="get" class="row g-2 align-items-end">
            <div class="col-md-3">
                <label class="form-label small">Search</label>
                <input name="keyword" value="@Model.SearchKeyword" class="form-control form-control-sm" placeholder="Student No or Name..." />
            </div>
            <div class="col-md-2">
                <label class="form-label small">Building</label>
                <select name="building" class="form-select form-select-sm">
                    <option value="">All</option>
                    @foreach (var b in Model.Buildings)
                    {
                        <option value="@b.BuildingName" selected="@(Model.FilterBuilding == b.BuildingName)">@b.BuildingName</option>
                    }
                </select>
            </div>
            <div class="col-md-2">
                <label class="form-label small">Year</label>
                <select name="year" class="form-select form-select-sm">
                    <option value="">All</option>
                    @for (int y = 1; y <= 6; y++)
                    {
                        <option value="@y" selected="@(Model.FilterYear == y)">@y</option>
                    }
                </select>
            </div>
            <div class="col-md-2">
                <label class="form-label small">Status</label>
                <select name="status" class="form-select form-select-sm">
                    <option value="">All</option>
                    <option value="Active" selected="@(Model.FilterStatus == "Active")">Active</option>
                    <option value="Left" selected="@(Model.FilterStatus == "Left")">Left</option>
                    <option value="Suspended" selected="@(Model.FilterStatus == "Suspended")">Suspended</option>
                </select>
            </div>
            <div class="col-md-3 d-flex gap-2">
                <button type="submit" class="btn btn-sm btn-primary"><i class="bi bi-funnel me-1"></i>Filter</button>
                <a asp-action="StudentReport" class="btn btn-sm btn-outline-secondary">Reset</a>
            </div>
        </form>
    </div>
</div>

<div class="card border-0 shadow-sm">
    <div class="table-responsive">
        <table class="table table-hover mb-0 align-middle">
            <thead class="table-light">
                <tr>
                    <th>Student No</th>
                    <th>Full Name</th>
                    <th>Program</th>
                    <th class="text-center">Year</th>
                    <th>Room</th>
                    <th>Building</th>
                    <th>Status</th>
                    <th>Check-in</th>
                    <th class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var row in Model.Rows)
                {
                    <tr>
                        <td class="fw-semibold">@row.StudentNumber</td>
                        <td>@row.FullName</td>
                        <td class="small text-muted">@row.Program</td>
                        <td class="text-center">@row.Year</td>
                        <td class="fw-semibold">@(row.RoomNumber ?? "-")</td>
                        <td>@(row.BuildingName ?? "-")</td>
                        <td>
                            @if (row.Status == "Active")
                            {
                                <span class="badge bg-success">Active</span>
                            }
                            else if (row.Status == "Left")
                            {
                                <span class="badge bg-secondary">Left</span>
                            }
                            else
                            {
                                <span class="badge bg-warning text-dark">@row.Status</span>
                            }
                        </td>
                        <td class="small">@(row.CheckInDate?.ToString("dd/MM/yyyy") ?? "-")</td>
                        <td class="text-center">
                            <div class="btn-group btn-group-sm">
                                <a asp-controller="Student" asp-action="Details" asp-route-id="@row.StudentId" class="btn btn-outline-primary" title="Details">
                                    <i class="bi bi-eye"></i>
                                </a>
                                <a asp-controller="Room" asp-action="AssignStudent" asp-route-studentId="@row.StudentId" class="btn btn-outline-success" title="Assign Room">
                                    <i class="bi bi-door-open"></i>
                                </a>
                                <a asp-controller="Payment" asp-action="StudentPayments" asp-route-studentId="@row.StudentId" class="btn btn-outline-info" title="Payments">
                                    <i class="bi bi-credit-card"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
                @if (!Model.Rows.Any())
                {
                    <tr>
                        <td colspan="9" class="text-center text-muted py-4">No students found.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
